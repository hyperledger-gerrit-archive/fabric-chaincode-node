{
  "name": "fabric-contract-api",
  "version": "1.4.0-snapshot",
  "tag": "unstable",
  "description": "A node.js implementation of Hyperledger Fabric chaincode shim, to allow endorsing peers and user-provided chaincodes to communicate with each other",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/hyperledger/fabric-chaincode-node"
  },
  "scripts": {
    "compile": "tsc --project test/typescript",
    "schema:test": "ajv compile -s ./schema/contract-schema.json && ajv validate -s schema/contract-schema.json -d schema/example-full.json"
  },
  "keywords": [
    "fabric-shim",
    "Hyperledger Fabric",
    "Fabric Shim"
  ],
  "engines": {
    "node": "^8.9.0",
    "npm": "^5.5.1"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "get-params": "^0.1.2",
    "reflect-metadata": "^0.1.12"
  },
  "types": "./types/index.d.ts",
  "devDependencies": {
  }
}
